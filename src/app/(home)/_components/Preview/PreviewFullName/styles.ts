import styled from "styled-components";
import { SkeletonFullName } from "../styles";

export const PreviewFullNameWrapper = styled(SkeletonFullName)`
  width: 100%;
  text-align: center;
  font-size: 1.8rem;
  font-weight: 600;
  color: #333;
  background-color: transparent;
`;
